;; domain file: domain-lights.pddl
(define (domain default)
    (:requirements :strips)
    (:predicates
    ( )
      
    )
    
    (:action right
        :parameters
        :precondition (
        )
        :effect (
        )
    )
)